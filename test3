for i,v in pairs(game:GetService("Workspace").Enemies["Reborn Skeleton"].Humanoid:GetDescendants()) do
    if v.ClassName == "Model" and v.Humanoid.Health >= 0 then
        v.Humanoid.Health = 0
    end
end
